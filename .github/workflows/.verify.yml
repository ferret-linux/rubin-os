# SPDX-License-Identifier: GPL-3.0-only
#
# This file is part of the rubin-os project:
#    https://github.com/ferret-linux/rubin-os
# Copyright (C) 2026 Ferret Linux contributors

name: verify (internal)

on:
  workflow_call:

jobs:
  verify:
    name: Verify ${{ matrix.image_name }}
    runs-on: ubuntu-26.04
    permissions:
      contents: read
      packages: read

    strategy:
      fail-fast: false
      matrix:
        include:
          - image_name: rubin-os
          - image_name: rubin-os-mx
          - image_name: rubin-os-gx
          - image_name: rubin-os-dx
          - image_name: rubin-os-vx
          - image_name: rubin-os-nvidia
          - image_name: rubin-os-mx-nvidia
          - image_name: rubin-os-gx-nvidia
          - image_name: rubin-os-dx-nvidia
          - image_name: rubin-os-vx-nvidia

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Login to GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | podman login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Install Cosign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2
        with:
          cosign-release: 'v2.6.3'

      - name: Verify Signature
        run: |
          for i in 1 2 3; do
            cosign verify \
              --key cosign.pub \
              ghcr.io/ferret-linux/${{ matrix.image_name }}:latest && break
            echo "Attempt $i/3 failed, retrying in 15s..."
            sleep 15
          done
        env:
          COSIGN_EXPERIMENTAL: false

      - name: Pull Image
        run: |
          for i in 1 2 3; do
            podman pull ghcr.io/ferret-linux/${{ matrix.image_name }}:latest && break
            echo "Attempt $i/3 failed, retrying in 15s..."
            sleep 15
          done

      - name: Run Image
        run: |
          podman run --rm ghcr.io/ferret-linux/${{ matrix.image_name }}:latest echo ok